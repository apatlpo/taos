#!/usr/bin/env python

from distutils.core import setup

#INSTALL_REQUIRES = ['xarray >= 0.10.6', ]

setup(name='taos',
      description='TAOS project python library',
      url='https://github.com/apatlpo/taos',
      packages=['taos'])

#      install_requires=INSTALL_REQUIRES,


